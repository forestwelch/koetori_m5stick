/*
 * Koetori M5StickC PLUS2 (BLE) - Secrets Template
 *
 * Copy to secrets.h and set DEVICE_ID. Used for BLE advertising name
 * "Koetori-M5-<DEVICE_ID>". iPhone app handles API keys and upload.
 */

#ifndef SECRETS_H
#define SECRETS_H

// Device ID - unique name for this M5Stick (used in BLE name and by app)
const char* DEVICE_ID = "m5stick-001";

// Optional: used in Debug > Device Info and can be sent to app if needed
const char* USERNAME = "yourname";

#endif
